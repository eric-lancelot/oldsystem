@model IEnumerable<v1jobportal.Models.EducationDetails>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameOfInstitution)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QualificationAttained)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateQualificationWasAttained)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameOfInstitution)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QualificationAttained)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateQualificationWasAttained)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
